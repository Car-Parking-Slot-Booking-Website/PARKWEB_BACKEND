Install these libraries on your cmd by using these commands
  -->npm install express mongoose body-parser stripe nodemon
Make an account on ngrok
Also Download ngrok ,then open its terminal .
Authenticate it by using the authentication syntax from the ngrok site.
now you can use any temporary url provided by ngrok to expose your private network to internet

just type on ngrok terminal:-
ngrok http PORT NUMBER

run the seed.js file first to seed database 
then run your server.js
then you are ready to go!
